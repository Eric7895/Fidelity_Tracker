server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # React Router handles routes such as /stocks/NVDA in the browser.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
